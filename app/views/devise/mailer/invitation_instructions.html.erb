<p><%= I18n.t("devise.mailer.invitation_instructions.hello", email: @resource.email) %></p>

<p>Invitation to Sign Up</p>
<p><%= link_to 'Click here', "#{Constants::FRONTEND_URL}/users/invitation/#{@token}/#{@resource.email}"%> to accept invitation and complete your registration process.</p>

<% if @resource.invitation_due_at %>
  <p><%= t("devise.mailer.invitation_instructions.accept_until", due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format')) %></p>
<% end %>

<p><%= t("devise.mailer.invitationo_instructions.already_accepted")%> then <%= link_to 'Login', "#{Constants::FRONTEND_URL}/users/sign_in"%></p>

<p><%= t("devise.mailer.invitation_instructions.mail") %></p>
<p><%= t("devise.mailer.invitation_instructions.tntra_team") %></p>
